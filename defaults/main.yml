---
iso_arch: "amd64"
# Valid values are: amd64, arm64

ssh_public_key: "-"

pikvm_address: "https://192.168.2.12/login/"

pikvm_username: "admin"

pikvm_password: "-"

target_dir: "{{ ansible_env.HOME }}/.local/ansible-proxmox_autoinstall"

enable_pikvm: false
