---
locale: "en-us.UTF-8"

iso_arch: "amd64"
# Valid values are: amd64, arm64
# ppc64el and s390x haven't been tested yet

keyboard_layout: "us"

hostname: "debian-autoinstall"

password: "debian"

username: "debian"

ssh_public_key: ""

pikvm_address: ""

pikvm_username: "admin"

pikvm_password: "admin"

target_dir: "{{ ansible_env.HOME }}/.local/ansible_debian-autoinstall"

boot_drive_serial:  "SABRENT_SABRENT_DB9876543214E"

grub_escape_seq: \\\

enable_pikvm: false
